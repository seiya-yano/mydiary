<div class="back-image">
  <div class="back-color_new">
    <p id="notice"><%= notice %></p>

    <div class="show">
    <p class="showp">
      <strong>作成日付:</strong>
      <%= @diary.created_at %>
    </p>

    <p class="showp">
      <strong>Title:</strong>
      <%= @diary.title %>
    </p>

    <p class="textauto">
      <%=safe_join(@diary.text.split("\n"),tag(:br))%>
    </p>
    </div>
    <div class="marginleft">
      <%= link_to '編集', edit_diary_path(@diary) %> |
      <%= link_to '一覧へ戻る', diaries_path %>
    </div>
  </div>
</div>